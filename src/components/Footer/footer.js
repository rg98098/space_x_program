import React from 'react';
import classes from './footer.css';

const footer = (props)=>(
    <div className={classes.footer}>
        <p><strong>Developed by :</strong> Rahul Gupta</p>
    </div>
);

export default footer;